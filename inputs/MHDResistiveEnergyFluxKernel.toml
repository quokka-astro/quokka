# *****************************************************************
# Dummy geometry/AMR entries: unused by the test itself (it never
# constructs a QuokkaSimulation), but required because AMReX parses
# this file before problem_main() runs.
# *****************************************************************
geometry.prob_lo = [0.0, 0.0, 0.0]
geometry.prob_hi = [1.0, 1.0, 1.0]
geometry.is_periodic = [1, 1, 1]

amr.v = 0
amr.n_cell = [16, 8, 8]
amr.max_level = 0
amr.blocking_factor = 8
amr.max_grid_size = 16

do_reflux = 0
do_subcycle = 0
