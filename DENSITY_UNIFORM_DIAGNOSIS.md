# 为什么密度图看起来完全均匀 - 诊断报告

## 核心问题
用户观察到：所有生成的密度切片图看起来完全均匀，没有任何SN（超新星）扰动的迹象。

## 当前结论
基于 yt 直接读取的结果，当前检查到的 density 场确实是常数场，因此“看起来均匀”不是渲染错误，而是数据本身没有可见起伏。

## 发现的关键信息

### 1. **时间坐标问题**（关键！）
从 yt 读取的 plotfile 时间在当前检查里并不一致，且早先 metadata.yaml 读出来是 0.00 Myr。

**问题：当前的时间轴不能直接用于把 CSV 里的 1300.58 Myr 和某个 density frame 一一对应。**

### 2. **SN事件时间与plotfile的不匹配**
- CSV 温度数据显示：曾被标记为 SN 的事件在 **1300.58 Myr**
- 当前 density 检查的 plotfile 帧并没有提供能稳定对应这一时刻的时间轴
- 因此不能把 frame 12 直接当作已验证的 SN 密度帧

### 3. **SN_count信号的困惑**
- metadata.yaml显示：`sn_count_cumulative: 1` 从plt0000010开始
- 但密度场仍然是常数场，说明 SN_count 信号不等价于可见的密度扰动
- 这表明当前至少存在一个“事件已记录，但密度没有显示结构”的分歧

## 可能的原因（按可能性排序）

### 可能1：**当前可视化帧并不是 CSV 里那个 SN 的同一时刻**（最可能 ⚠️）
- 密度图的 frame 与 CSV 的时间轴尚未严格对齐
- 因此“密度完全均匀”并不等于“没有 SN”，而是“当前帧没有显示出 SN 结构”

### 可能2：**时间记录系统需要重新校验**
- metadata.yaml 曾经全部是 0.00 Myr
- 当前需要重新从 Header / 运行日志 / 其它来源建立时间映射

### 可能3：**SN主要体现在温度或金属丰度，而不是密度**
- 当前已验证的 density 场是常数场
- 如果要找 SN，应优先检查 temperature 或 metallicity 的同帧变化

### 可能4：**网格分辨率太低**
- 当前分辨率：32×32×32 = 32,768 个网格点
- 即使有局部扰动，也可能被切片压平到看不出来

## 检查清单

### ✓ 已验证的事实
- [x] Plotfile确实存在（30个文件，Apr 25生成）
- [x] yt可以读取 plotfile
- [x] density 场在检查帧里是常数
- [x] CSV 中存在温度异常记录

### 🔍 需要检查的内容

1. **重新对齐时间轴后再看 Metallicty（金属丰度）**
   - 如果 SN 主要是化学富集，metallicity 可能比 density 更敏感
   - 先确认同一时刻再比较

2. **检查非常早期的 frames（frame 0-2）**
   - 确认初始条件是否就是常数场
   - 这有助于判断后续“均匀”到底是物理结果还是数据问题
   
3. **重新检查 Temperature 时间序列**
   - 不要把当前 density 帧直接和 CSV 的 1300.58 Myr 绑定
   - 先把时间轴校正再看温度曲线

4. **验证Code Units**
   ```python
   # 检查初始密度值
   # 应该和initial conditions匹配
   # n0 = 1.0e5 cm^-3 (从ChuhanProblem.toml)
   ```

5. **运行时诊断**
   ```bash
   # 重新检查ChuhanProblem.toml中的参数
   # 特别是：
   # - problem_max_time / 模拟停止时间
   # - initial_dt 和 max_timesteps
   ```

## 物理解释

### 为什么SN可能看不到密度变化
1. **Type II SN的两个相位**
   - **第1相（膨胀）**：气体膨胀，密度下降（应该看到暗点）
   - **第2相（冷却）**：金属富集气体冷却（应该看到metallicity增加）

2. **可能的情景**
   - 如果看到的是冷却后的SN（膨胀已经完成）
   - 密度会接近背景值
   - 但金属丰度会升高

3. **低分辨率的影响**
   - 32³分辨率下，SN的Sedov-Taylor球可能只占1-2个网格点
   - 无法看到清晰的结构

## 推荐的后续行动

### 立即检查（5分钟）
1. 查看 metallicity 图是否显示任何亮点，但先确认时间对齐
2. 提取 frame 10 的数据统计（min/max/mean density）
3. 比较 frame 0 vs frame 10 的平均 density

### 调查代码（15分钟）
1. 检查 ChuhanProblem.cpp 中的事件注入代码
2. 确认运行日志里记录的时间是否和 plotfile 对得上
3. 查看能量/金属注入方式是否只影响温度/标量而不影响密度

### 验证配置（10分钟）
1. 确认 ChuhanProblem.toml 中的 max_timesteps 是否真的增加了
2. 重新运行一次，检查日志输出的最后时间
3. 验证 plotfile_interval 是否正确设置

## 总结

```
问题：          密度图看起来完全均匀
原因（候选）:   
   1. 时间没有对齐，当前帧不是同一时刻
   2. density 本来就是近似常数
   3. SN 主要体现在温度/金属丰度
   4. 分辨率太低

快速验证：      先修正时间轴，再检查 metallicity 和 temperature
```
