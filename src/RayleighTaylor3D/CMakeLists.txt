if (AMReX_SPACEDIM EQUAL 3)
    add_executable(test_hydro3d_rt ../main.cpp ../CloudyCooling.cpp ../GrackleDataReader.cpp test_hydro3d_rt.cpp ${primordial_chem_sources})
    if(AMReX_GPU_BACKEND MATCHES "CUDA")
        setup_target_for_cuda_compilation(test_hydro3d_rt)
    endif()
endif()
