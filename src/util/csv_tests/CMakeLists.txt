find_package(Python COMPONENTS Interpreter REQUIRED)
add_executable(CSVReaderTest testCSVReader.cpp)
target_link_libraries(CSVReaderTest PRIVATE AMReX::amrex ${HDF5_LIBRARIES})
target_include_directories(CSVReaderTest PRIVATE ../.. ${HDF5_INCLUDE_DIRS})
target_compile_features(CSVReaderTest PRIVATE cxx_std_20)
if(AMReX_GPU_BACKEND MATCHES "CUDA")
  setup_target_for_cuda_compilation(CSVReaderTest)
endif()
add_test(NAME CSVReaderTest COMMAND ${Python_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/test_csv_reader.py $<TARGET_FILE:CSVReaderTest>)
